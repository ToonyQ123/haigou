server:
  port: 10086
spring:
  application:
    name: hg-registry
eureka:
  client:
    service-url:
      defaultZone: http://10.12.159.40:${server.port}/eureka
    fetch-registry: false
    register-with-eureka: false
  server:
    enable-self-preservation: false
    eviction-interval-timer-in-ms: 5000
